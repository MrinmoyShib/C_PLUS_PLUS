#include <cmath>
#include <iostream>

int main() {
  double x = 3, y = 4, z;
  /*z = std::max(x, y);
  z = std::min(x, y);
  z = pow(2, 3);
  z = sqrt(9);
  z = abs(-7);
  z = round(x);
  z = ceil(x);
  z = floor(x);*/
}